# Changelog

Finding gap의 릴리스 단위 변경 이력입니다.
버전은 이 파일과 git 태그로만 관리하며, **웹페이지에는 노출하지 않습니다.**
형식은 [Keep a Changelog](https://keepachangelog.com/ko/), 버전 체계는 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [Unreleased]

### 기능
- **분류 이력 전문가 제보 — 논문 근거(DOI) 기반.** GBIF 자동 체크(v0.11.0)는 국제 백본에만 의존해 국내 학술지 전용 분류 개정을 못 잡는다. 로그인 사용자가 종 상세에서 "학명이 이 논문 때문에 바뀌었다"를 제보하면 관리자 검수 후 "이명 확인됨" 배지(GBIF 자동 매치인 "이명 가능성"보다 진한 색으로 신뢰도 차이를 구분)로 반영된다. 시민 제보(`reports`) 기능을 구조 그대로 복제 — 새 테이블 `taxon_reference_reports`(본인 행 RLS + 관리자 승인), 새 모달(`#refOverlay`)과 마이페이지 관리자 검토 큐(기존 `#adminCard` 옆에 병렬 추가).
  - DOI를 입력하면 **Crossref API**(무료·키 불필요·CORS 허용, 실측 확인)로 제목·저자·연도·저널을 자동으로 끌어와 채운다 — 서지 정보도 자체 포맷을 만들지 않고 학술 인프라 표준(DOI/Crossref)에 기대는, GBIF taxonomicStatus 어휘를 그대로 쓴 것과 같은 원칙.
  - **논문 PDF를 올리면 DOI를 자동으로 찾아** 위 Crossref 조회까지 이어서 실행한다 — `pdf.js`(esm.sh CDN, 기존 EXIF 라이브러리 `exifr`와 동일한 로드 방식)로 브라우저에서 직접 첫 2쪽 텍스트만 뽑아 DOI 패턴을 찾는다(실제 논문 PDF로 추출 확인). 못 찾으면 DOI 직접 입력으로 폴백. **"무엇이 무엇으로 바뀌었다"는 주장 자체는 자동 추출하지 않고 사람이 입력**하게 남겨뒀다 — 이 기능의 신뢰도(전문가가 직접 확인)를 기계 추론으로 낮추지 않기 위함.
  - `build_gap_reason_snapshot.py`가 `fg_taxon_status_check`(자동)와 `taxon_reference_reports`(승인된 제보)를 합쳐 종당 사유를 하나로 결정 — 우선순위 **reference(전문가 확인) > regionally_extinct > synonym**(사람이 특정 논문으로 확인한 것이 가장 신뢰도 높음).
  - 승인 큐 접근은 별도 역할 없이 기존 `profiles.role='admin'`을 그대로 재사용 — 지금 규모에서 새 권한 체계는 과함.
  - `5_App/supabase/taxon_reference_reports.sql` Supabase 적용 완료(2026-08-19) — 테이블·RLS 정책 4개 실측 확인.

## [0.11.0] - 2026-08-19

### 수정
- **주소로 들어온 값이 잘못됐을 때 조용히 넘어가던 문제.** 두 가지다. (1) `?sido=abc` 처럼 없는 지역 코드를 그대로 선택으로 삼아, 어느 지역도 걸리지 않는 빈 표를 지역이 골라진 것처럼 보여 줬다 — 시군구(`?sigungu=`)는 이미 다섯 자리 숫자인지 확인하고 있었는데 시도만 그 확인이 빠져 있어, 같은 규칙(두 자리 숫자)을 시도에도 걸었다(실측: `?sido=abc` → 선택 0곳·전국 88종, `?sido=11` → 서울 선택 그대로). (2) `?sp=` 로 넘어온 종 번호가 목록에 없으면 아무 일도 하지 않아, 링크를 누른 사람은 첫 화면을 보며 무엇이 잘못됐는지 알 수 없었다 — 이제 종별 화면으로 옮긴 뒤 "주소에 적힌 종을 찾지 못했습니다. 이름으로 찾아보세요."를 보인다.
- **표에 걸리는 종이 하나도 없을 때 머리글만 남던 문제.** 연도나 지역을 좁히다 결과가 0이 되면 열 이름만 있는 빈 표가 남아, 자료가 없는 것인지 조건을 잘못 고른 것인지 구분할 수 없었다. 종 검색에는 이미 "검색 결과가 없습니다."가 있었으므로 분류군 표에도 같은 자리를 채우되, 지금 보고 있는 지표에 맞춰 말한다("이 조건에서 발견된 종이 없습니다" / "이 조건에서 미발견 종이 없습니다" + "연도 범위나 지역 선택을 바꿔 보세요").
- **배포본 조립이 원본 하나만 없어도 산출 폴더를 비운 채 멈추던 문제.** `build_dist.py` 는 출력 폴더를 먼저 비우고 목록에 적힌 것만 다시 복사한다. 자료 목록은 파일이 있는지 확인하고 없으면 경고만 남기는데 페이지 목록에는 그 확인이 없어, 하나라도 빠지면 그 자리에서 예외로 끝나 폴더가 절반만 찬 채 남았다. 두 목록을 같은 방식으로 맞췄다.
- **종별 화면에서 "내 위치"를 누르면 분류군 전체 화면으로 튕기던 문제.** 작은땃쥐 지도를 보다가 내 위치를 누르면 포유류 전체 조회로 되돌아가, 보고 있던 종이 사라졌다. 이 버튼이 원래 모드 A(분류군)의 기계 — 지역 선택·발견공백 지표·표 다시 그리기 — 를 그대로 쓰려고 화면부터 A 로 바꾸고 있었다. 위치를 물었을 뿐 화면을 바꿔 달라고 한 것이 아니므로, 지금은 보고 있던 화면을 그대로 두고 그 시군구만 비춘다. 모드 B 에서는 알림도 그 종의 말로 바뀐다("수원시 장안구 · 작은땃쥐 기록 없음"). 모드 A 의 동작은 그대로다(선택 `31011` · 지표 `발견공백` · "수원시 장안구 발견공백").
- **모든 이벤트에 `anonymize_ip=true` 가 매개변수로 딸려 나가던 문제.** 측정 로더가 `anonymize_ip` 를 켜 두고 있었는데, 이것은 예전 애널리틱스의 설정 이름이라 GA4 는 알아보지 못한다. 알아보지 못한 설정은 버려지는 것이 아니라 **그 이벤트의 매개변수로 실려 나가서**, 방문·검색·조회 할 것 없이 모든 기록에 쓰이지도 않는 값이 하나씩 붙었다. GA4 는 IP 를 저장하지 않아 익명화가 기본이므로 지워도 잃는 것이 없다.

### 기능
- **지역·분류군 요약 페이지 154쪽에 측정 코드 추가.** 검색으로 들어오는 입구가 이 페이지들인데 정작 측정 코드가 빠져 있어, 어느 지역·어느 분류군이 실제로 읽히는지 한 건도 잡히지 않았다. 낱장 153쪽과 목록 1쪽 전부에 조회 화면과 같은 로더를 붙였다 — 측정 ID 가 없으면 아무 요청도 하지 않는 성질도 그대로다.
- **참여 행동 세 가지를 측정에 추가.** 지금까지는 로그인 벽에 부딪힌 것까지만 남고 그 뒤가 비어 있어, 벽을 넘은 사람이 얼마나 되는지 알 수 없었다. 로그인 성공·관심 종 추가와 빼기·제보 등록을 각각 남긴다. 로그인은 **새로 로그인한 때만** 세고 열어 둔 세션이 되살아난 것은 세지 않는다 — 인증 이벤트 이름을 함께 넘겨 둘을 가른다.
- **지도에서 시도·시군구를 누르면 그 지역으로 확대.** 지금까지 지역 클릭은 표를 거르기만 하고 지도는 전국 범위에 머물러, 정작 고른 지역이 화면에서 손톱만 하게 남았다. 두 화면 모두에 넣었다 — 분류군 화면은 고를 때 함께 확대되고(전남 선택 시 6.6 → 8.62), 종별 화면은 선택 기계가 없으므로 확대만 한다(선택·종 상태 그대로). **해제하거나 폴리곤 밖을 눌렀을 때는 지도를 움직이지 않는다** — 선택을 지우는 동작이 화면까지 되돌리면 무엇이 지워졌는지 확인할 자리가 사라진다(같은 지역 재클릭 시 8.62 유지). 이미 그 지역보다 깊이 들여다보고 있으면 뒤로 물러나지 않고, 되돌아가는 길은 기존 `전국` 단추가 그대로 맡는다. 확대는 애니메이션 없이 즉시 맞춘다 — 탭이 뒤로 가 프레임이 멈추면 진행 중이던 이동이 끝나지 않은 채 남고 그 뒤의 이동이 전부 줄을 서서 지도가 멈춘다(실측: `getAnimating()` 이 참으로 고정).
- **지도 위쪽에 지역 검색 추가.** 지도를 눌러 찾아 들어가는 길만으로는, 이름은 아는데 그것이 지도의 어디쯤인지 모르는 지역에 닿을 수 없었다. 시도 17 + 시군구 252 = **269곳**을 색인해 이름·초성으로 찾고, 고르면 그 행정구역으로 확대한다(종로구 → 줌 10, 제주특별자치도 → 9.4). 초성은 종 검색과 같은 규칙이라 따로 배울 것이 없다(`ㄱㅇ` → 강원특별자치도·계양구 등 14곳). 경계 자료의 시군구 이름에는 시도가 빠져 있어 **중구·남구·동구처럼 여러 시도에 흩어진 이름은 그대로면 가릴 수 없다** — 오른쪽에 소속 시도를 함께 적었다(`중구` → 서울·부산·대구·인천 등 6곳). 지도는 두 화면이 하나를 옮겨 쓰므로 컨트롤 하나가 양쪽에서 그대로 뜬다. 키보드는 ↑↓·Enter·Esc 로 다룬다.
- **지도 위 컨트롤을 한 줄 띠로 통합.** 분류군 선택·분류군 복귀·지역 검색·서식지 후보·지역 경계 토글이 저마다 따로 지도 위에 떠 있어, 폭이 줄어들면 서로의 타격영역을 잠식했다(881px 실측 시 가운데 남는 폭 136px). 서식지 후보는 홀로 좌하단에 있어 44px 확대·축소 단추 위에 폭이 세 배쯤 되는 상자가 얹힌 꼴이라 어느 묶음에도 속해 보이지 않았다. 이제 다섯을 왼쪽부터 한 띠에 담는다 — 분류군(종별 화면에서는 복귀) → 지역 검색 → 서식지 후보 → 오른쪽 끝에 지역 경계 토글. 남는 폭은 검색칸이 흡수하고(156~272px), 한 줄에 다 담기지 않으면 줄만 바뀐다. 서식지 후보는 종별 화면에서만 뜨므로 두 화면의 띠가 저절로 갈린다.
  - 띠의 빈 자리는 지도가 받아야 그 위에서 끌어 옮길 수 있는데, OpenLayers 가 컨트롤 요소에 `pointer-events:auto` 를 **인라인으로** 박아 스타일시트로는 되돌릴 수 없었다(실측: 더 구체적인 규칙을 줘도 계산값이 `auto`). 스크립트에서 되돌린 뒤 띠 한가운데의 히트테스트는 지도(`ol-layers`)다.
- **모바일 분류군 선택을 아이콘 + 분류군명 단추로.** 좁은 화면에서는 아이콘 한 칸만 남기고 투명한 선택칸을 덮어 브라우저 기본 팝업을 띄우고 있었는데, 아이콘만으로는 지금 무엇을 보고 있는지 읽히지 않았다. 이제 단추에 아이콘과 이름이 함께 뜨고(85.3×44px), 목록도 직접 그려 **줄마다 아이콘 + 이름**을 보인다(9줄 각 44px, 지금 보고 있는 분류군에 ✓, 수집예정은 흐리게 두고 고를 수 없다). 목록은 지도 상자가 아니라 본문에 얹고 문서 좌표로 박는다 — 지도 안에 두면 아래쪽이 잘려 뒤쪽 분류군을 고를 수 없고(모바일 지도 높이 400px, 목록 392px), 문서 좌표라 화면을 굴려도 단추를 따라간다(실측: 120px 굴렸을 때 단추·목록 모두 −120px, 간격 4px 유지). 아래가 모자라면 위로 편다(짧은 화면 실측: 목록 아래끝 321px < 단추 위끝 326px). 키보드는 ↑↓·Home·End·Enter·Esc 로 다루고 스크린리더에는 목록 상자로 읽힌다.
- **종별 화면의 분류군 복귀 단추는 좁은 화면에서 아이콘만.** 이름(`← 포유류 전체`)까지 두면 같은 줄에 지역 검색이 들어설 폭이 남지 않았다. 44×44 아이콘 단추로 줄이고 이름은 툴팁과 스크린리더 이름으로 남긴다("포유류 전체 화면으로 돌아가기").
  - 1280px·375px 양쪽 실측 — 1280px 종별 화면은 복귀(128.5px) · 검색(272px) · **검색칸 오른쪽 8px 지점의 서식지 후보**(97.7×44px) · 경계 토글이 한 줄에 서고, 375px 은 복귀 44 · 검색 193 · 서식지 후보 44 가 첫 줄, 경계 토글이 둘째 줄 오른쪽 끝에 붙는다. 가로 넘침 0(문서 폭 375), 콘솔 오류 0건. 회귀 확인: 종별 화면에서도 지역 검색이 종로구로 확대되고 화면은 그대로 종별에 머무르며, 서식지 후보 토글이 `aria-pressed` 와 함께 왕복하고, 분류군을 바꾸면 `fTaxon` 만 따라오고 `sTaxon` 은 빈 값을 유지한다.
- **지도 밖 `← 분류군 전체` 버튼 제거.** 지도 좌상단 복귀 버튼이 들어오면서 같은 일을 하는 단추가 둘이 되었다 — 검색줄 오른쪽의 글 버튼은 지도를 들여다보는 동안 시야 밖이라 돌아갈 길로 읽히지 않으면서, 화면에는 계속 남아 어느 쪽이 정식 통로인지 흐렸다. 돌아가는 길은 지도 좌상단 하나로 모은다(검색줄에는 공유 단추만 남는다).
- **마이페이지에서 관심 종을 한 번에 여러 종 담고, 표로 본다.** 지금까지 관심 종을 담는 길은 종별 화면의 ☆ 하나뿐이라, 열 종을 담으려면 종을 찾아 들어갔다 나오기를 열 번 해야 했다. 목록 오른쪽 위에 `＋ 종 추가` 를 두어 마이페이지를 떠나지 않고 검색으로 여러 종을 고른 뒤 한 번에 담는다(실측: 3종을 골라 담아 10 → 13줄, 담긴 뒤 고른 것은 비워지고 단추는 다시 잠긴다). 목록은 국명·학명·분류군·발견 현황이 열로 서는 표로 바꾸고, **담아 둔 종에 실제로 있는 분류군만** 골라 거를 수 있게 했다(분류군이 하나뿐이면 고를 것이 없으므로 감춘다. 실측: 관속식물 한 종을 해제하자 그 단추가 사라졌다).
- **관심 종 줄에서 바로 제보.** 줄마다 지도에서 보기·제보·해제를 둔다. 지금까지는 제보하려면 그 종의 화면까지 다시 찾아 들어가야 했는데, 이제 이름 옆 단추 하나로 그 종이 대상으로 채워진 제보창이 열린다(실측: 수달 줄의 제보 → 대상 종 `수달 Lutra lutra` · 멸종위기 안내 함께 표시).
- **활동 지역을 여러 곳 담을 수 있게.** 한 곳만 고를 수 있어, 사는 곳과 자주 가는 곳이 다른 사람은 둘 중 하나를 포기해야 했다. `＋ 지역 추가` 로 여러 곳을 담고, 담아 둔 곳은 시·군·구 / 시·도 / 최근 10년 발견 / 아직 기록 없음 이 열로 서는 표로 본다. 줄마다 분류군별 보기와 해제를 둔다(실측: 3곳 담김 → 강릉시 해제 후 2곳, 이미 추가한 곳을 다시 고르면 "이미 추가한 지역입니다"). 활동 지역 알림도 **담아 둔 곳마다 따로** 센다(실측: 계절 안내와 공백 증가 알림이 종로구·용산구 각각 생성).
  - 이 값은 표가 아니라 로그인 정보에 딸린 설정이라 자료 구조를 바꾸지 않고 목록으로 넓혔다. 예전에 한 곳만 고른 사람은 목록이 없으므로 그 한 값을 목록으로 읽고, 저장할 때는 목록의 첫 곳을 예전 자리에도 함께 적는다 — 한 곳만 읽던 자리가 그대로 동작한다(실측: 목록 없이 단일 값만 가진 계정도 지역 1곳이 표에 뜨고 알림도 정상). 첫 설정 화면에서 지역을 고를 때도 이미 담아 둔 곳을 지우지 않고 앞에 더한다(실측: 서귀포시를 고르자 `["39020","11010","11030"]`).
  - 1280px·375px 양쪽 실측 — 좁은 화면에서는 두 표 모두 학명·시·도 열을 접고 단추를 44px 로 키운다. 가로 넘침 0(문서 폭 375, 표는 자기 상자 안에서만 가로로 움직인다), 콘솔 오류 0건.
- **지역 검색 기록에 어느 지역인지 함께 남김.** 지금까지는 "시도까지 봤다 / 시군구까지 파고들었다" 는 깊이만 남고 정작 **어느 지역**을 찾았는지가 비어 있어, 검색으로 어디를 궁금해하는지 알 수 없었다. 이제 고른 지역 이름을 함께 남긴다. 이름은 목록에 뜨는 것을 그대로 쓴다 — 경계 자료의 시군구 이름에는 시도가 빠져 있어 `중구` 만으로는 여섯 곳이 겹치기 때문에, 시도를 붙인 `서울 종로구`·`경기` 형태로 남긴다(실측: 종로구 → `sigungu`·`서울 종로구`, 경기도 → `sido`·`경기`).
- **발견공백 사유 태깅(이명·지역절멸) — 종 상세 배지 반영.** "미발견"이라는 판정 하나에 성격이 다른 현상이 섞여 있다 — 진짜 조사부족일 수도 있지만, KTSN 학명이 GBIF Backbone Taxonomy 기준 이미 이명(synonym) 처리된 분류학적 잔재이거나, 국가적색목록상 이미 국내에서 지역절멸(RE) 판정된 종이라 애초에 찾을 대상이 아닐 수도 있다. 종 상세 화면에 "이명 가능성"/"국내 지역절멸 기준종" 배지를 추가해 이를 구분해서 보여준다(발견공백 집계 자체를 바꾸지는 않음 — 정보성 표시). 전종 체크 결과 **이명 가능성 2,605종·지역절멸 12종**.
  - 판정 근거는 자체 라벨이 아니라 **Darwin Core `taxonomicStatus` 어휘**(ACCEPTED/SYNONYM/HOMOTYPIC_SYNONYM/…)를 그대로 저장 — GBIF·Catalogue of Life 등 해외 생물종 DB와 같은 언어를 쓴다.
  - 신규 Supabase 테이블 `fg_taxon_status_check`(append-only, `5_App/supabase/taxon_status_check.sql`) — GBIF/COL이 이름을 지우지 않고 이력을 쌓듯, 판정을 UPDATE하지 않고 매 체크마다 새 행을 쌓는다. "현재 상태"는 `(ktsn, source)`별 최신 `checked_at` 행으로 조회 — 나중에 GBIF 백본이 갱신돼 판정이 바뀌어도 예전 판정이 지워지지 않고 이력으로 남는다.
  - `7_MCP/check_gbif_synonyms.py --push`로 전종(38,622건) 체크 + Supabase 적재 완료(분류군 단위 `--taxon-group` 실행·지역절멸 `--redlist` 포함).
  - `7_MCP/build_gap_reason_snapshot.py`(Supabase → `gap_reason.json`) → `5_App/build_gap_reason_asset.py`가 분류군별 지연 로드 자산(`gapreason_<T>.js`, 기존 `model_<T>.js`와 동일 관례)으로 쪼개 프런트에 전달 — 종 상세 배지는 이미 반영·배포 가능한 상태.
  - MCP 스냅샷(`build_mcp_data.py` → `fg_mcp.sqlite.gz`)도 다시 만들어 `species.gap_reason`·`gap_reason_note` 를 실었다 — 이제 화면 배지뿐 아니라 MCP 도구(`get_species` 등)와 챗봇 답에도 같은 사유가 함께 나온다(재생성 후 실측: 이명 2,605종·지역절멸 12종으로 전종 체크 결과와 일치, 나머지 37,355종은 해당 없음).
  - **이번 범위에서 하지 않음**: 지역/분류군 발견공백 총계(대시보드·`region_gaps.js`) 재계산, 조사자 편향 정량화(별도 진행 중인 P0), 오동정/오기록 플래그, 오탐 수동 정정 UI.

### 개편
- **본문 서체를 Pretendard 로 교체.** 지금까지는 서체를 지정하지 않아 윈도우에서는 맑은 고딕, 안드로이드에서는 노토, 맥에서는 애플 SD 산돌고딕으로 각각 그려졌다 — 기기마다 다른 화면이 나오는 것이 "통일감이 없다"는 인상의 실체였다. 맑은 고딕은 굵기가 두 단(400·700)뿐이라 CSS 로 500·600 을 지정해도 400 이나 700 으로 접혀, 강조가 살지 않았다. 실측으로 확인한 차이 두 가지: (1) 15px 굵기 사다리 400/600/700 이 Pretendard 에서는 133.92 / 136.50 / 137.77px 로 세 단이 갈리는데 맑은 고딕은 146.66 / 150.61 / **150.61**px 로 600 과 700 이 같다. (2) 같은 13px 본문 한 문장이 298.00px vs 348.16px — 줄이 14.4% 짧아져 좁은 화면에서 접히는 줄이 줄어든다. jsdelivr 의 동적 서브셋(`pretendardvariable-dynamic-subset.min.css`, 유니코드 구간별 92개 `@font-face`)을 쓰므로 실제로 화면에 나온 글자에 해당하는 조각만 내려받는다. 모든 조각이 `font-display:swap` 이라 서체가 늦게 와도 글은 먼저 보이고, 내려받기가 실패하면 지금까지 쓰던 스택(system-ui → 맑은 고딕)으로 되돌아간다.
- **글자 크기 19단 → 6단, 굵기 5단 → 3단.** 크기가 10·10.5·11·11.5·12·12.5·13·13.5·14·15·16·17·18·20·22·23·24·26·30px 로 열아홉 가지 섞여 있어, 0.5px 차이가 위계를 뜻하는지 그때그때의 실수인지 코드만 봐서는 구분되지 않았다. 여섯 단(11 메타·각주 / 12 표·칩·조밀한 컨트롤 / 13 본문 / 15 강조 본문·카드 제목 / 19 절 제목 / 24 화면 제목)과 세 단(400 본문 / 600 강조·제목 / 700 최상위)으로 정리했다(크기 173곳, 굵기 7곳). 좁은 화면에서 데스크톱과 같은 값이 되어 버린 재선언 5건은 지웠다. 숫자가 열로 서는 여섯 곳(집계 수·좌표 등)에는 `font-variant-numeric:tabular-nums` 를 넣어 자릿수가 흔들리지 않게 했다.
- 서체 교체 과정에서 **같은 뿌리의 결함 세 개**가 드러나 함께 고쳤다. (1) `font:` 단축 표기 6곳은 가족을 명시적으로 되돌리므로, 본문만 바꾸면 지도 컨트롤·범례 등은 맑은 고딕으로 남는다 — 단축을 유지한 채(풀면 `line-height` 가 `normal` 로 빠져 버튼 높이가 흔들린다) 가족만 끼워 넣었다. (2) 단추·입력칸·선택칸은 서체와 크기를 물려받지 않아 **40개 요소가 Arial(한글은 설치된 기본 고딕) 13.33px 로** 남아 있었다 — `button,input,select,textarea{font-family:inherit;font-size:inherit}` 로 닫았다. (3) `body` 에 크기가 없어 규칙이 닿지 않은 글이 브라우저 기본값 16px 로 떨어졌다 — 검색 결과의 국명이 실제로 그렇게 그려지고 있었다. `body{font-size:13px}` 로 못박고 결과 국명은 15px 로 지정했다. `body` 를 13px 로 고정하면 크기를 `em` 으로 잡는 OpenLayers 기본 컨트롤이 20px → 18px 로 줄어들어, 데스크톱에서 원래 크기를 지키는 하한을 함께 걸었다(좁은 화면의 44px 규칙은 그대로).
  - 1280px·375px 양쪽에서 실측 확인 — 화면에 뜬 글 가운데 Pretendard 가 아닌 것 0건, 여섯 단·세 단 밖으로 나간 글과 컨트롤 0건(OpenLayers 자체 글리프 제외), 가로 넘침 0, 콘솔 오류 0건. 회귀 확인: 지도 확대·축소·저작권·서식지 후보·도움말·분류군 복귀 버튼이 모두 자기 자신으로 히트테스트되고, 종 검색과 종별 화면 진입이 정상이며 `sTaxon` 은 빈 값을 유지한다.
- **같은 정리를 종 퀴즈 화면(`quiz.html`)과 넘김 페이지 두 곳에도 적용.** 조회 화면만 바꾸면 서비스 안에서 화면을 옮길 때 서체가 갈린다 — 퀴즈는 조회 화면의 카드와 종별 화면의 "동정 연습" 버튼에서 바로 이어지는 자리다. 퀴즈 화면도 같은 서체·같은 여섯 단(크기 40곳)·세 단(굵기 5곳)으로 맞추고, 좁은 화면 재선언 3건은 데스크톱과 값이 같아져 지웠다(제목과 종 국명의 24 → 19px 축소는 유지). 화면 제목의 `<b>` 는 브라우저가 `h1`(700)에 한 단을 더 얹어 **이 한 곳만 900** 으로 그리고 있었다 — 이름을 가르는 일은 이미 색이 하고 있으므로 굵기는 물려받게 했다. `chat.html`·`service.html` 은 곧바로 넘어가는 페이지라 웹폰트를 내려받지 않고, 자동 이동이 막혔을 때만 보이는 한 줄에 같은 대체 스택만 준다(그냥 두면 브라우저 기본 명조로 그려졌다).
  - 크기를 올리자 **사진 위 출처 자막이 사진을 덮는 문제**가 드러났다. 갤러리 썸네일은 100px 남짓한 정사각인데 자막에 줄 수 제한이 없어, 출처가 긴 사진에서 넉 줄까지 늘어나 **108px 상자의 42%** 를 덮었다(9 → 11px 로 올리기 전에도 이미 35% 수준). 두 줄로 자르고 잘린 전문은 마우스를 올리거나 사진을 눌러 여는 확대 화면에서 온전히 보이게 해, 출처 길이와 무관하게 **30% 로 묶었다**(짧은 출처는 한 줄 18%).
  - 1280px·375px 양쪽에서 퀴즈를 실제로 풀어 가며 실측 — 분류군 퀴즈(문제·선택지·정답 배너)와 종 퀴즈(사진 8장 비교·학습 화면·갤러리) 모두 Pretendard 아닌 글 0건, 단 밖으로 나간 글 0건, 가로 넘침 0, 콘솔 오류 0건. 선택지 50px·탭 44px 로 타격 영역도 그대로다.
- **지도 안 지역 컨트롤(시도·시군구·전국·ON/OFF)을 키보드와 스크린리더에서 쓸 수 있게 정리.** 이 네 단추는 탭으로 닿기는 하는데 **초점 표시가 아예 없어**(`outline-style:none`) 지금 어디에 있는지 알 수 없었고, 화면에 뜬 글자가 두세 자뿐이라 스크린리더로는 "시도"·"ON" 만 읽혀 무엇을 가르는 단추인지 알 수 없었다. (1) 안쪽으로 그리는 초점 링을 넣었다 — 테두리가 없는 단추라 바깥으로 빼면 이웃 단추에 가려 잘린다(대비 8.82:1, 켜진 단추는 흰 링으로 뒤집어 같은 값). (2) 두 묶음에 `role="group"` 과 이름(지역 경계 수준 / 지역 경계 표시)을 달아 단추 이름 앞에 함께 읽히게 하고, 켜짐 여부를 `aria-pressed` 로 밝혀 레벨 전환·표시 토글 양쪽에서 갱신되게 했다(`전국` 은 상태가 아니라 동작이라 붙이지 않았다). (3) 높이 24 → 32px — 지도 위에 겹쳐 뜨는 컨트롤이라 키워도 본문이 밀리지 않는다(좁은 화면은 44px 규칙 그대로).
- 모달·패널 닫기(×) 색을 `#8a8f8c` → `--muted` 로(3.29 → 4.83:1). 24px 글리프라 큰 글씨 기준(3:1)은 통과하고 있었으나 본문 기준에는 못 미쳤다. 함께, 뒤에 온 같은 선택자에 통째로 덮여 한 번도 적용되지 않던 죽은 규칙 2건(`footer`·`.btn:disabled`)을 지웠다 — 값은 실측으로 변화 없음을 확인(footer 24/12/12px 유지).
  - 1280px·375px 양쪽 실측 — 네 단추 모두 자기 자신으로 히트테스트(1280px 32px, 375px 44px), 지도 확대·축소 버튼 44×44 정상, 가로 넘침 0, 콘솔 오류 0건. 회귀 확인: 레벨 전환과 경계 표시 토글이 `aria-pressed` 와 함께 왕복하고, 종 검색·종별 화면 진입 정상, `sTaxon` 빈 값 유지, 종 카드 높이 961px(1280px)·1,073px(375px)로 이전과 같다.
- **종별 화면 아래쪽의 분류군 요약을 감춤.** 종 하나를 보러 들어왔는데 지도 아래에 그 종이 속한 분류군 전체의 이야기 - 지표 카드 넷, 전국 발견 현황 막대, 국가적색목록 도넛 - 가 그대로 이어 붙어 있었다. 화면 길이의 절반 가까이가 지금 보고 있는 종과 무관한 내용이라, 정작 그 종의 정보는 맨 위 한 장에서 끝났다. 이제 이 요약은 분류군 화면에서만 뜬다. 실측: 종별 화면 길이가 휴대폰(375px)에서 3,950 -> 2,500px(4.9화면 -> 3.1화면), 데스크톱(1280px)에서 2,914 -> 2,262px 로 줄었다. 지우는 것이 아니라 감추는 것이라 분류군으로 돌아가면 요약이 그대로 다시 뜬다(복귀 후 높이 635px 유지). 가로 넘침 0, 콘솔 오류 0건.
- **관심 종·활동 지역을 더하는 말을 "담기" 에서 "추가" 로 통일.** 목록을 여는 단추는 `＋ 종 추가`·`＋ 지역 추가` 인데 정작 그 안에서 누르는 단추는 `담기` 라, 같은 동작을 화면 한 곳에서 두 이름으로 부르고 있었다. 짝이 되는 `닫기` 와도 한 글자 차이라 좁은 화면에서 잘못 누르기 쉬웠다. 단추와 안내 문구를 모두 `추가` 로 맞췄다(`이미 추가한 지역입니다`·`3종 추가했습니다`·`추가 실패: `). `닫기` 는 그대로 둔다. 첫 화면의 집계 위젯 제목도 `많이 담긴 종` → `많이 추가된 종` 으로 따라 바꿨다.

### 문서
- **저장소 전체의 문서와 주석을 한 벌로 정비.** 처음 보는 사람이 코드만 읽고도 무엇이 어디서 도는지 알 수 있게 맞췄다. 루트 `README.md` 는 서비스가 세 층 - 브라우저가 직접 읽는 정적 자산, Supabase, 공개 MCP 서버 - 으로 나뉜다는 것과 각 층이 멈추면 무엇이 안 되는지를 먼저 적는다(정적 자산이 끊기면 조회가 멈추고, Supabase 가 끊겨도 조회는 그대로 된다). `5_App/index.html` 은 2,500줄이 넘는 한 파일이라 머리에 파일 지도를 붙였다 - 스크립트 덩어리가 각각 무엇을 맡는지, 고치기 전에 알아야 할 규칙이 무엇인지. 새 문서 둘을 더했다. `5_App/supabase/README.md` 는 SQL 파일 여덟 개를 어떤 순서로 적용하고 무엇이 무엇에 기대는지, `3_ETL/analysis/README.md` 는 VIF 산출물이 보고용 변수 선택 기록이며 서비스 모형이 실제로 쓰는 변수 목록(`3_ETL/R/model_config.R` 의 `V`)과는 다르다는 점을 밝힌다. 빌드 스크립트 13개와 `run_pipeline.py`, MCP `tools.py` 의 머리말도 같은 기준으로 다시 썼다. 화면과 동작에 닿는 코드는 한 줄도 바꾸지 않았다.
- **`VERSION` 이 0.9.0 에 멈춰 있던 것을 0.10.0 으로.** 이 파일과 CHANGELOG 가 버전의 단일 출처인데 둘이 어긋나 있었다.
- **기능 진행 현황 문서가 실제와 어긋난 곳을 바로잡음.** `6_Deliverables/FUTURE_PLAN.md` 는 발표와 보고서가 근거로 삼는 문서인데, 적어 둔 뒤로 코드만 앞서 나가 여러 곳이 사실과 달라져 있었다. 공개 MCP 도구 표는 아홉 줄이었으나 실제로 도는 것은 **열다섯 개**고 그중 이름이 맞는 것은 하나뿐이었다 — 같은 목록을 두 곳에 적어 두면 또 어긋나므로, 이름과 인자의 단일 출처는 `7_MCP/README.md` 로 두고 이 문서에는 무엇을 할 수 있는지만 갈래로 남겼다. 관심 종을 담아 두는 표 이름을 `favorite_species` 로 적어 두었으나 실제로 쓰는 것은 `watchlist` 이고(`5_App/supabase/auth_baseline.sql`), `favorite_species` 는 종마다 메모를 붙이려고 설계만 해 둔 다른 표다 — 둘을 갈라 적었다. "일부 도구는 로그인이 필요하다" 고 되어 있었으나 열다섯 개 모두 인증 없이 쓸 수 있다. 이 밖에 관리자 페이지 설명이 실제로는 Phase 1 에 있는데 Phase 2 를 가리키던 것, 종의 상태를 뜻하는 말(`미발견`)을 기능이 없다는 뜻으로 쓴 것, 검토만 하고 쓰지 않은 호스팅(Cloudflare Pages)을 운영 중으로 적어 둔 것, 오타(`데이터 정신성`)를 고쳤다. 8월 18일부터 도는 방문·검색 측정은 현황 표에 빠져 있어 한 줄 더했다.

### 정리
- **어디서도 참조하지 않는 파일 정리.** 예제 데이터 `species_media.sample.js` 와, 서비스에서 빼기로 한 미삭동물(UC)의 `cells_UC.js`·`season_UC.js` 를 지웠다. 셋 다 배포본이 싣지 않던 것이라 화면은 그대로다. 다만 UC 두 개는 `build_gap_data.py`·`build_model_data.py` 가 종 마스터의 분류군 목록을 그대로 훑어 만들기 때문에 다음 데이터 재생성 때 다시 생긴다 - 그때 다시 지우거나 두 스크립트에 서비스 분류군 필터를 넣어야 한다.
- **자리를 옮긴 것 여섯.** `geo_00_sido_geojson.R` 은 나머지 R 스크립트가 모여 있는 `3_ETL/R/` 로, VIF 산출물 다섯은 파이프라인 단계로 오해받지 않게 `3_ETL/analysis/` 로 옮겼다. 이름만 남고 내용이 없던 `3_ETL/sql/`·`6_Deliverables/diagrams/` 는 지웠다.

## [0.10.0] - 2026-08-17

### 개편
- **지도 좌상단에 분류군 전체 복귀 버튼 추가.** 종별 화면에서 분류군 전체로 돌아가는 길이 지도 위쪽의 작은 글 버튼(`← 분류군 전체`) 하나뿐이라, 지도를 들여다보는 동안에는 시야에 들어오지 않아 돌아갈 길이 없는 것처럼 보였다. 분류군 화면에서 분류군 선택 상자가 쓰는 자리(지도 좌상단)를 종별 화면에서는 이 버튼이 이어받고, **보고 있는 종이 속한 분류군의 아이콘**을 함께 실어 어디로 돌아가는지까지 아이콘으로 알린다(포유류 종을 보면 포유류 아이콘). 상자를 분류군 색으로 채우지 않은 것은 대비 때문이다 — 주황(`#D98C36`) 위 흰 글자는 2.71:1 로 기준(4.5:1)에 한참 못 미친다. 상자는 흰색으로 두고 색은 아이콘 칩이 맡는다. 좁은 화면(≤880px)에서는 오른쪽 위 토글 상자가 빈 곳에서도 클릭을 가져가므로, 이름이 긴 분류군(무척추동물(곤충제외))은 겹치기 전에 잘라 내고 전체 이름은 마우스를 올렸을 때 뜨게 했다(1280px·375px 양쪽에서 버튼이 자기 자신으로 히트테스트되는 것을 확인).
- 홈 4탭(조사정보/대화형/시민과학/마이데이터) → 2탭으로 통합. (1) 시민과학+마이데이터를 "시민과학·마이데이터" 한 탭으로 — 이미 하나의 JS 모듈로 구현돼 있어 HTML 래퍼만 합침(`#view-community`), `#my` 딥링크는 통합 탭의 마이데이터 칸으로 스크롤. (2) 대화형 도우미를 4번째 탭에서 떼어내 조사정보 탭 옆의 **슬라이드 패널**(`#chatDrawer`, 데스크톱 우측/모바일 하단시트)로 — 지도가 서비스의 핵심이라 대화형이 이를 가리지 않고 "옆에서 돕는 조수"로 보이게 함. `.tabs`에 💬 토글 버튼 추가, `#chat` 딥링크는 패널을 열도록. 챗봇의 "지도에서 보기"는 데스크톱은 패널 유지, 모바일은 패널을 닫고 지도를 바로 보여줌.

### 기능
- **"미발견 후보" → "서식지 후보"로 개편.** (1) 이름을 바꾼 것은 표현 문제가 아니라 판정 대상이 달라졌기 때문 — 이 층은 "아직 발견되지 않은 곳"이 아니라 "환경이 맞는 곳 중 기록이 없는 칸"이다. (2) 켜고 끄는 자리를 **지도 안 좌하단 레이어 토글**로 옮기고, 발견/미발견 이중 버튼을 없앰 — 두 층을 겹쳐 보는 것이 원래 쓰임인데 버튼이 화면을 갈라 한쪽만 보게 만들고 있었다. 발견 지점과 **같은 줌(9 이상)** 에서 함께 뜬다. (3) 범례의 "기록 없음" 칸을 서식지 후보가 대신함. (4) 토글 옆 `?` 로 판정 근거를 그 자리에서 설명. 후보 색은 짙은 주황(`#A85A00`) + 별표.
- **서식지 후보 판정을 종별 MaxEnt 모형으로 교체**(기존 BIOCLIM 엔벨로프 대체). 엔벨로프는 변수마다 상자를 그려 겹치는 부분을 남기는 방식이라 변수 사이 관계를 보지 못하고 상자 모서리 조합까지 적합으로 넘겼다. 이제 발견지점과 전국 배경(dem·ndvi·bio01·bio03·bio14·bio18)을 대조해 학습한 계수를 브라우저가 그대로 계산한다(cloglog, 105,340칸 · 종당 0.1초 안팎). 자산은 분류군별 지연 로드(`model_<T>.js`·`env_model.js`).
- 서식지 후보의 **적합 경계를 종마다 다르게** — 고정 기준선(교차검증 TSS 0.4 미달 제외)을 버리고 종별 최적 임계값을 쓴다. 고정선으로는 8,935종 중 4,086종이 통째로 사라졌는데, 미달 원인은 표본이 얇아서가 아니라 분포가 넓어서였다(점유 1,000칸 이상 구간 83.0% 미달 vs 10~29칸 구간 20.4% 미달). 전국에 고루 사는 종은 환경으로 갈라낼 것이 애초에 적다. 결과적으로 **4,849종 → 8,929종**으로 확대. 대신 값을 감추는 대신 근거의 세기를 드러내도록 **신뢰 등급(A~E)** 을 범례·설명에 함께 표기(등급의 자는 아래 항목에서 교차검증 AUC 로 확정).
- **신뢰 등급(A~E)에 "이게 무슨 뜻인지" 설명 패널 추가.** 등급만 A~E로 띄워 두고 근거를 어디에도 밝히지 않아, 낮은 등급이 "자료가 부실하다"는 뜻인지 "이 종은 원래 가려내기 어렵다"는 뜻인지 알 수 없었다. 등급 배지를 누르면 채점 방식(발견한 자리를 넷으로 나눠 셋으로 배우고 남은 하나로 맞혀 보기), 다섯 등급의 뜻, 그 종이 실제로 쓴 변수 목록, 그리고 **낮은 등급이 나오는 이유가 대개 종의 성질**(전국에 고루 사는 종은 환경으로 갈라낼 것이 애초에 적다)이라는 점을 평이한 말로 설명한다. 채점이 무작위 분할이라 **처음 가 보는 지역에 대해서는 실제보다 후하게 나온다**는 한계도 함께 밝힌다.
- **물에서 사는 종의 서식지 후보에 수계 마스크 적용.** 모형이 쓰는 변수(기온·강수·고도·식생)에는 물이 있는지가 들어 있지 않아, 어류·저서성무척추동물도 하천 옆 산비탈이 "기후가 맞다"는 이유로 후보가 됐다. DEM에서 유도한 시도별 하천망(`4_References/<시도>/Channel Network_*.shp` 9개, 105,651개 구간·총 107,668km)을 통합해 1km 격자와 교차시키고(`3_ETL/python/build_cell_water.py` → `cell_water.csv`), 하천이 지나는 칸만 남기는 마스크를 **판정이 끝난 격자에 덧씌운다**. 환경격자의 새 변수로 넣지 않은 이유는 모형 설정 해시가 달라져 8,900여 종을 전부 다시 적합해야 해서다. 전국 105,340칸 중 84,927칸(80.6%)이 수계 셀이며, 마스크 대상은 모형이 있는 수생종 684종. 문턱은 Strahler 차수 1 이상 — 올리면 실제 어류 발견 셀까지 잘려 나간다(어류 점유 셀 15,829칸 기준 ≥1 90.3% 잔존 vs ≥2 76.1%, ≥3 49.8%). 격자 CRS·셀 크기(EPSG:5186 · 989.999m)는 연속 `cid` 변위로 실측해 확정. 후보 설명에 "물에서 사는 종이라 하천이 지나는 칸만 남겼습니다" 문장이 함께 뜬다.
- **물에서 사는 종에게 하천의 크기(Strahler 차수 `sord`)를 모형 변수로 추가.** 수계 마스크는 "하천이 있는 칸/없는 칸"만 갈랐을 뿐, 실개천과 큰 강을 구분하지 못했다. 그래서 큰 강에만 사는 종의 후보가 산속 1차 계류까지 퍼졌다. `cell_water.csv` 가 이미 셀별 최대 차수를 담고 있었는데 있음/없음 한 비트로 접어 쓰고 있었다. 이제 `env_grid_model.csv` 에 `sord` 열을 싣고, `ndwi_species.csv` 에 오른 수생종 2,019종에게만 변수를 하나 더 배정한다(`model_config.R` 의 `WVAR`). 육상종의 변수 구성은 그대로다.
  - 채택 근거는 TSS 상승이 아니라 **위치 정확도**다. 함께 검토한 다섯 갈래 중 변수를 넉넉히 주는 쪽(구간 규칙 해제)은 무작위 교차검증에서 +0.0340 이 나왔지만 **공간 교차검증에서 +0.0070 으로 79%가 증발**했다 — 조사가 몰린 자리를 외운 것이지 환경을 배운 것이 아니었다. `sord` 는 무작위 +0.0507 · **공간 +0.0595 로 오히려 늘어** 남았다. 표적군 배경(TGB)은 공간겹에서 −0.0186 으로 기각.
  - 실제 결과 차이(수생종 684종 시험 적합 기준): 후보 27,384 → 24,593칸으로 좁혀지고 종별 겹침(Jaccard) 중앙 0.674 — 58.0%는 좁아지고 41.2%는 넓어졌다. **후보가 실제 서식 하천 규모에서 떨어진 거리가 0.423 → 0.237 차수(63.4% 근접, p=2.1e-35)**, 큰 강 종 41종은 100% 가까워졌다. TSS 중앙 0.456 → 0.526, 신뢰 등급은 189종 상승 · 10종 하락.
  - 변수 구성이 바뀌면 `model_cfg` 해시가 달라져 **전 종 재적합**이 필요하다. 9개 분류군 **8,942종**을 다시 적합했고(약 10시간, 실패 12종) 배포 자산은 **8,930종**을 싣는다 — 수계 마스크 633종 · 해산종 52종. 저장소가 한 전제로만 적합됐는지는 `build_model_data.py` 가 자산을 쓰기 전에 `cfg` 해시로 확인한다(갈리면 중단). 신뢰 등급 분포 A 916 · B 1,395 · C 2,413 · D 3,163 · E 1,043.
  - 재적합 후 대조해 보니 **육상종 계수도 미세하게 달라졌다** — 원인은 `sord` 가 아니라 격자의 정밀도다. 직전 모형 저장소는 배포 정밀도 반올림(`round(x·SCALE)/SCALE`)이 들어오기 전에 적합돼 원시 정밀도 값으로 학습했는데, 브라우저는 언제나 반올림된 `env_grid.js` 값으로 채점해 왔다(예: `bio03` 학습 20.7559 vs 채점 20.8). 이번 재적합으로 **학습값과 채점값이 처음으로 일치**한다.
  - **화면에 실제로 뜨는 후보가 얼마나 달라졌는지**를 배포 격자(105,340칸)에서 옛 계수·새 계수로 각각 채점해 실측했다. 두 갈래를 갈라 봤다 — 육상종은 변수 구성이 그대로라 위 정밀도 차이만 남고(대조군), 수생종은 `sord` 가 새로 들어갔다. 해산종 323종은 양쪽 다 후보가 없어 제외.

    | | 육상종 8,119종 | 수생종 630종 |
    |---|---|---|
    | 후보 겹침(Jaccard) 중앙 | 1.000 (하위 10% 0.978) | 0.679 (하위 10% 0.419) |
    | 후보의 30% 넘게 바뀐 종 | 0.1% | 52.4% (절반 넘게 24.9%) |
    | 후보칸 변화 중앙 | +0칸 (넓어짐 2,022 · 좁아짐 2,011) | −574칸 (넓어짐 268 · 좁아짐 358) |
    | 교차검증 AUC 변화 중앙 | ±0.0000 (오름 1,249 · 내림 1,254) | +0.0215 (오름 518 · 내림 103) |
    | 신뢰 등급 이동 | 7종 (0.1%) | 221종 (35.1%) |

    육상종은 넓어진 종과 좁아진 종이 2,022 대 2,011 로 갈려 방향이 없다 — 반올림 잡음이고, 화면에서 알아볼 수 있는 수준이 아니다. 그 대조군이 있으므로 수생종의 한쪽 쏠림(좁아짐 358 대 넓어짐 268, AUC 오름 518 대 내림 103)은 `sord` 의 효과로 읽힌다. **후보가 점수만 오른 것이 아니라 맞는 하천 규모로 좁혀졌다**는 뜻이다. 후보가 통째로 사라진 종은 양쪽 다 0, 새로 생긴 종은 수생종 3. 한쪽 저장소에만 있는 종은 신규 1 · 사라짐 6.
  - 라이브에서 보이는 **신뢰 등급 변화는 두 가지가 겹친 결과**다 — 등급의 자를 TSS 에서 AUC 로 바꾼 것(아래 항목, 재적합과 무관)과 이번 재적합. 두 변경이 같은 배포에 함께 실린다.
- **신뢰 등급의 자를 교차검증 TSS 에서 교차검증 AUC 로 교체**(경계 0.9/0.8/0.7/0.6). 고정 TSS 기준선을 버린 이유가 등급에는 그대로 남아 있었다 — TSS 는 분포 넓이에 딸려 움직여서(로그 점유칸과 ρ=−0.492) 전국에 고루 사는 종이 모형 품질과 무관하게 낮은 등급을 받았다. 점유 10~29칸 종의 TSS 중앙 0.538 vs 1,000칸 이상 0.262 인 반면, AUC 는 0.724 vs 0.664 이고 상관은 −0.099 다.
  - 판단 근거는 관례가 아니라 **공간 블록 교차검증 대조**다. 900종을 0.5°(약 50km) 블록으로 다시 나눠 "가 본 적 없는 지역"을 맞히는 실력을 재고, 두 지표가 그것을 얼마나 예고하는지 비교했다. 화면에 실제로 뜨는 것 — **상위 10% 후보의 정밀도를 AUC 가 더 잘 예고**(ρ +0.742 vs +0.687, 상위 5%는 +0.659 vs +0.598)한다. TSS 가 앞서는 두 항목(공간 TSS +0.921, 재현율 +0.552)은 TSS 자신의 편향을 공유하는 결과값이다 — 공간 TSS 도 점유칸과 ρ=−0.362 로 묶여 있는 반면 상위 10% 정밀도는 −0.018 로 넓이와 무관하다.
  - 존재-배경 AUC 는 분포가 좁을수록 거저 오른다는 함정이 있어 반증을 걸었고, **기각**됐다. 점유칸 구간을 고정해도 우위가 유지되고(10~29칸 +0.680 vs +0.648, 30~99칸 +0.793 vs +0.761), 점유칸 수만으로는 정밀도가 전혀 예측되지 않는다(ρ −0.018). 두 지표를 순위평균으로 섞으면 어느 항목에서도 단독보다 낫지 않아 결합안은 버렸다.
  - 관례 경계에서 등급이 성능을 단조로 가른다 — 공간겹 상위 10% 정밀도 A 0.719 · B 0.450 · C 0.276 · D 0.157 · E 0.083. E 비중이 5.7% → 11.8% 로 늘지만 그 집단의 실측 정밀도가 0.083(전체 중앙 0.233)이라 과한 강등이 아니고, 내려가는 종은 점유 10~11칸으로 표본이 얇아 TSS 만 좋아 보이던 것들이다.
  - `auc_cv` 는 이미 모형 저장소에 있어 **재적합 없이** 바뀐다. 자르는 임계값은 그대로 종별 최대-TSS 지점을 쓴다 — 자를 위치는 운영 결정, 등급은 모형 품질 진술이라 다른 자를 써도 모순이 아니며, 설명 패널에 그 구분을 밝힌다. 점수 설명도 "맞힌 비율 − 헛짚은 비율"에서 **"발견 지점 한 곳과 아무 곳 한 곳을 나란히 놓고 어느 쪽이 서식지인지 맞힐 확률, 0.5는 동전 던지기"** 로 바꿔 더 쉬운 말이 됐다.
- 발견 제보 근거: URL 입력 대신 **사진 업로드**를 1순위로 지원 — 선택한 사진의 EXIF GPS로 발견 위치 자동입력(없으면 지도 클릭), 업로드 전 클라이언트 압축(최대 1600px·JPEG q0.82). URL 입력은 "사진이 없다면" 폴백으로 유지.
- 사진은 Storage 버킷 `report-photos`(본인 폴더만 업로드/삭제, 읽기는 public)에 저장. `reports.url`은 선택 컬럼으로 변경, `photo_path` 컬럼 추가(둘 중 하나는 필수).

- 발견공백 도우미(chat)에 **강·목·과·속(class/order/family/genus) 단위 질의** 지원 — "사슴벌레과에 아직 기록되지 않은 종은?" 같은 질문에 응답. 과·속의 한글 분류명은 `fg_taxon_name`(taxon_ko.js 기반)으로 라틴명 해석(강·목은 한글 매핑 없어 라틴명만). KTSN 마스터 분류 단계가 강-목-과-속-종/아종뿐이라 아과·족 등은 지원 범위 밖.
- 발견공백 도우미: **분류군 질의 커버리지 전체화 + 발견공백 순위 도구** — (1) `fg_taxon_name`을 KTSN 전체(강·목·과·속 4계층)로 확대해 한글명 해석률을 과 60→96%·속 42→69%로 높이고 강·목도 한글 질의 지원(`7_MCP/build_taxon_names.py` → `taxon_names.json.gz`), (2) 한글명 정확 일치 실패 시 `pg_trgm` 유사도로 후보(`suggestions`) 제시(딱따구리↔딱다구리 등 철자변형·'나비' 통칭 완화), (3) 신규 도구 `taxon_gap_ranking` — "곤충류에서 미발견 종 많은 과", "전남에서 한 번도 기록 안 된 과" 등 과·속 단위 발견공백 순위. (4) 분류군명 해석을 **검증된 라틴명만 쿼리에 사용**하도록 강화(사전 정확일치→실데이터 라틴 존재확인→퍼지 근사 순, 어디에도 없으면 조회 미실행·후보만 반환), 근사 해석 시 `approximate`/`matched_taxon`으로 "가장 비슷한 분류군으로 안내".

- 4개 페이지(index/service/quiz/chat)에 파비콘 링크 + OG/Twitter 공유 메타 태그 추가 — 공유 이미지는 `build_profiles.py`가 만드는 `og.png`로 site 공통 통일(지역·분류군 SEO 페이지와 동일 이미지).
- 파비콘·OG 카드를 실제 헤더 로고(`4_References/finding_gap_logo.png`) 기반으로 교체 — 처음엔 자체 격자 지도 디자인(`gen_brand_icon.py`)으로 시작했다가, 정식 로고가 나온 뒤 그걸로 통일. `build_logo.py`가 헤더 로고·파비콘류(favicon.ico·apple-touch-icon·icon-512)·`og.png`를 모두 같은 원본에서 생성 — 워드마크 앞 배지만 잘라 파비콘류로, 로고 전체를 1200×630 카드로. `og.png`는 `build_profiles.py`의 `make_og()`가 빌드 때마다 다시 그리므로(정적 파일 아님) 로고를 못 찾으면 예전 텍스트형으로 자동 폴백. 벡터 원본이 없어 `favicon.svg`는 제거(ico·png만).

### 버그 수정
- **어류 1,378종 전체가 "적합 모형이 만들어지지 않았습니다"로 뜨던 문제** — 어류의 분류군 코드는 `-P`인데 파일명으로 쓸 수 없는 `-`를 `_`로 씻어 `model__P.js`를 만들면서, 자산 안의 키까지 파일명 그대로 `"_P"`로 넣고 있었다. 클라이언트는 `"-P"`로 찾으니 어류 모형만 통째로 닿지 않았다(다른 8개 분류군은 코드에 `-`가 없어 무사). `ktsn_master.csv`의 실제 분류군 코드에서 파일명을 거꾸로 되짚어 키를 복원. 어류 181종의 서식지 후보가 이제 표시된다.
- 종별 검색(조사정보 탭)에서 검색이 전혀 동작하지 않던 문제 — `service.html`·`chat.html`이 `index.html`로 통합되면서 종 검색창과 대화형 입력창이 둘 다 `id="q"`를 쓰게 됨. 문서에 같은 id가 두 개 있으면 `window.q`가 그 엘리먼트 하나가 아니라 HTMLCollection이 되어, 검색창에 이벤트 리스너를 붙이는 코드(`q.addEventListener(...)`)가 페이지 로드 시 조용히 에러를 내고 죽어있었음. 대화형 입력창 id를 `chatQ`로 분리해 해결.
- 발견 제보 사진 입력에서 `capture="environment"` 제거 — 모바일에서 무조건 카메라만 뜨고 앨범(저장된 사진)을 고를 수 없던 문제. 속성을 빼면 브라우저가 카메라 촬영·앨범 선택을 모두 제공.
- 카카오톡·네이버 블로그 공유 미리보기가 새 로고 OG 카드로 안 바뀌고 예전 이미지를 계속 보여주던 문제 — 원인은 공유 대상 페이지 URL이 아니라 **`og.png` 이미지 URL 자체**가 카카오/네이버 쪽에 캐시돼 있던 것(이미지 URL이 안 바뀌니 파일 내용이 바뀌어도 캐시가 안 갱신됨). 모든 페이지(index/quiz/153개 지역 SEO 페이지+허브)의 `og:image`·`twitter:image`를 `og.png?v=2`로 캐시버스팅.
- **서식지 후보를 못 내는 사유를 실제로 문턱이 된 값으로 안내.** 화면은 관측 기록 수를 10과 비교했지만, 모형이 성립하느냐를 가르는 것은 서로 다른 발견 지점(1km 점유 셀)의 수다. 둘은 크게 어긋난다 — 모형이 있는 8,929종 기준 기록 수가 점유 셀보다 중앙값 27 많고, 최대 60,014 차이가 난다. 그래서 기록 99건·지점 9곳인 종이 "발견 지점은 충분한데 모형이 성립하지 않았다"는 틀린 이유를 보고 있었다(기록≥10인데 모형이 없는 962종 중 605종이 실제로는 지점 부족). 이제 지점 수로 판정해 "발견된 지점이 9곳뿐"이라 말한다. 물에서 사는 종은 지점이 충분한데 적합이 안 된 경우 자료의 한계(기후·지형·식생만으로 물길 환경을 담기 어려움)를 따로 밝힌다 — 이미 만들어 배포하던 `gap_meta.ndwi` 목록을 화면이 쓰지 않고 있었다.
- **시민 제보 집계(MCP `community_discoveries`)에 공개 하한 누락** — 공개 읽기 도구 중 유일하게 K-익명 하한이 없었다. (종 × 시군구) 조합에 제보가 한둘이면 익명 집계라도 그 지역에서 그 종을 제보한 사람으로 좁혀진다. 관심종 집계(`MIN_PUBLIC_WATCH`)와 같은 이유로 `MIN_PUBLIC_REPORT = 3` 을 도입해 행 조회·요약 집계 양쪽에 적용하고, 반환값의 `discoveries`·`total_reports` 도 하한을 넘긴 조합만 세도록 했다. 회귀 시험 추가(하한 준수 + 좌표·개인정보 미노출 불변식).
- MCP 데이터셋 생성일(`meta.generated`)이 손으로 고치는 상수라 실제 빌드보다 24일 뒤처진 채 배포된 적이 있다. 자료 신선도를 알리는 유일한 표시인데 어긋나면 이용자가 알 길이 없다. 빌드일 자동 기록으로 바꾸고, 재현 빌드가 필요할 때만 `FG_MCP_GEN_DATE` 로 고정하도록 했다.
- **해산 어종의 서식지 후보가 내륙 하천에 찍히던 문제.** 수생종 목록(`ndwi_species.csv`)의 '어류'는 종 마스터의 분류군이 `-P` 인 종 **전부**라 참돔·고등어·홍어 같은 바닷물고기가 함께 들어 있다. 이들에게 하천 마스크를 씌우면 후보가 바다에서 내륙 하천으로 옮겨 붙는다 — 점베도라치 한 종만 해도 **없는 곳 10,612칸**을 가리키고 있었다. 이 서비스의 1km 격자는 육지만 덮으므로 바다에 사는 종은 후보를 나타낼 칸 자체가 없다. 별도 명단 대신 **자기 발견 기록이 하천 밖에 몰린 종**(50% 이상)을 관측으로 가려내 후보를 만들지 않고 그 이유를 화면에 밝힌다(`build_model_data.py` 의 `_sea_species`, 수생종 2,019종 중 323종). 명단은 종이 늘 때마다 손봐야 하지만 "제 기록이 하천 밖에 있다"는 사실은 자료가 늘어도 스스로 맞는다. 경계 50%는 실측으로 잡았다 — 위쪽은 전부 해산 어류 과(科)이고, 40~50% 구간부터 물잠자리·물땡땡이처럼 하천망에 안 잡히는 웅덩이에 사는 담수종이 섞이기 시작한다.
- **지도 축소(−) 버튼이 눌리지 않던 문제** — 확대·축소 컨트롤은 기본 위치(좌상단)가 아니라 지도 좌하단에 놓여 있는데, 뒤에 붙인 `서식지 후보` 토글이 같은 자리를 잡으면서 축소 버튼을 통째로 덮었다(실측: 916×600 지도에서 축소 버튼 y 545~590 vs 토글 y 557~591 — 버튼 중심을 눌러도 축소가 아니라 토글이 눌렸다). 토글을 확대·축소 스택 위로 올렸다. 오른쪽으로 비키는 방법은 좁은 화면에서 막힌다 — 375px 에서 토글 옆 물음표의 말풍선(210px)이 지도 오른쪽 끝까지 15px 밖에 안 남아 조금만 밀어도 잘린다. 1280px·375px 양쪽에서 확대·축소·토글·물음표 네 버튼이 각각 자기 자신으로 히트테스트되는 것을 확인했다.
- 서식지 후보 표에서 **신뢰 등급 표가 좁은 화면을 밀어내던 문제** — 등급 설명의 표(340px 이상)가 375px 화면의 본문(343px)을 넘겨 글줄이 잘렸다. 표만 옆으로 스크롤되도록 감쌌다. 같은 화면에서 등급 배지의 누를 수 있는 높이도 28px → 44px 로(다른 조작 버튼과 같은 기준).

### 수정
- 발견공백 도우미(chat v9) **정확성·성능 개선** — (1) `list_species_by_taxon` 의 발견상태(state) 필터를 `LIMIT` **뒤 후처리**에서 **SQL 내 판정·필터**로 이동. 종전엔 국명순 상위 N개만 뽑은 뒤 걸러 "미발견 몇 종?"에 잘린 수를 답하고 뒤쪽 종이 누락됐다(예: 고치벌과 미발견 1,295종인데 ≤30만 보고). 이제 `count`=실제 전체 수, `species`=국명순 상위 표본, `state_totals`=발견/휴면/미발견 내역을 함께 반환. (2) 전국(지역 미지정) 집계를 종별 사전집계 MV(`fg_species_national`, ≈20k행)로 전환해 `fg_species_region`(590k) 전량 GROUP BY 제거(`taxa_summary`·`list_species_by_taxon`·`taxon_gap_ranking`). (3) 지역 한정 경로에 커버링 인덱스(`region/sido, ktsn, maxyear`) 추가 → index-only. (4) 분류군명 해석의 한글 정확·근사 조회를 단일 쿼리로 병합(왕복 3→2, 우선순위 정확>라틴>근사 보존). (5) `taxon_gap_ranking` 의 불필요한 `count(distinct)` 제거.
- 발견공백 도우미(chat v10): **하루 사용 횟수 제한 폐지.** 종전엔 1인 하루 20회를 넘기면 "오늘 사용 한도를 모두 사용했습니다"로 막았으나, 조사 계획을 세우는 실사용에서 20회는 쉽게 닿는 수치라 정상 이용까지 차단했다. 이제 평상시 사용은 제한하지 않고, 자동화된 대량 호출로 무료 사용량이 한 번에 소진되는 것만 막는 안전판(`CHAT_ABUSE_CAP`, 기본 300)을 둔다. 응답에서 잔여 횟수(`remaining`) 필드도 제거(화면에 표시하지 않던 값).
- `load_reference.py`: `--only <table[,table]>` 선택 적재 옵션 추가(소규모 갱신 시 590k행 재적재 생략), `fg_species_region` 재적재 시 전국 롤업 MV 자동 `REFRESH`.
- **종별 조회 순위를 집계할 수 있도록 계측 보강.** 종 상세를 열 때 보내던 `species_open` 이벤트는 종 식별자를 사용자 지정 매개변수(`ktsn`·`taxon`)로 담는데, 이 값은 수집은 되어도 **맞춤 측정기준으로 등록하기 전까지 어떤 보고서에도 뜨지 않고 등록해도 소급되지 않는다.** 등록 전까지 쌓인 조회는 순위로 쓸 수 없다는 뜻이라, 등록 여부와 무관하게 처음부터 보고서에 잡히는 표준 측정기준(페이지 경로·제목)으로도 같은 신호를 남기도록 `fgPage(경로, 제목)` 가상 페이지뷰를 추가했다(`?sp=<ktsn>` · 제목은 종명). 화면만 바뀌고 실제 이동이 없는 단일 페이지 구조라 그냥 두면 종 조회가 전혀 기록되지 않는다. 경로는 배포본이 하위 경로에 놓이는 것을 고려해 현재 주소 기준 상대경로로 해석한다.
- CSV 내려받기 2종(분류군 종 목록 · 종별 발견 시군구)에 `download` 이벤트 추가 — 어떤 자료가 실제로 쓰이는지 볼 수 있는 유일한 경로인데 아무 계측이 없었다.

### 제거
- **지도의 환경변수 오버레이 제거**(연평균기온·최난월·최한월·연강수·해발고도·NDVI·NDWI 7종). 화면에서는 앞선 변경으로 이미 빠져 있었는데 기록이 남지 않아, 0.9.0 의 지도 설명이 없는 기능을 계속 가리키고 있었다(그 문장은 이 항목으로 대체된다). 되살리는 대신 **정식 제거**로 정리한다 — 서비스가 답하는 질문은 "이 종이 어디서 발견됐고 어디가 비어 있는가"이고, 환경변수 자체를 색으로 덮어 보여 주는 층은 그 질문에 답하지 않는다. 같은 자료를 필요한 자리에서 이미 쓰고 있다(종 카드의 기후·고도 지위 막대, 서식지 후보 판정). 남아 있던 자산·생성 경로를 함께 걷어냈다 — 배포본의 오버레이 PNG 7장(트리당 2.17MB), `env_meta.js` 의 `layers` 블록, `env_layers.R` 의 PNG 렌더링과 `env_layers_meta.csv`, `build_dist.py` 의 `env/` 복사. 기후·고도 막대(`ENVMETA.vars`·`ref`)와 서식지 후보(`env_model.js`)는 다른 자산을 쓰므로 영향이 없다(배포 후 `demo/data/env/` 요청 0건 확인).

### 문서
- `3_ETL/DATA_PIPELINE.md` 실행 순서를 실제 코드와 맞춤. 재빌드는 **두 체인**(관측 ETL · 환경·모형 자산)인데 한 덩어리로 적혀 있었고, `run_pipeline.py` 의 12단계 중 `species_cells`·`cell_sigungu`·`ndwi_sp`·`cell_water`·`gap_data`·`env_grid_model`·`season`·`model`·`model_data` 아홉 단계가 아예 빠져 있었다(수계 마스크 단계는 문서 어디에도 없었다). 단계별 산출물 표와 **갱신 주기** 표를 추가하고, `env_layers.R` 산출을 실제 파일명(`species_env_stats.csv`·`env_grid.csv` 등)으로 고침 — 문서와 R 헤더 주석 모두 만들어지지 않는 `species_dem.csv` 를 가리키고 있었다.
- `3_ETL/README.md` 전면 개정. 만들어진 적 없는 구조(parquet 중간산출·Supabase 서빙·GitHub Actions cron)를 6단계 표로 설명하고 있었다. 실제 진입점(두 체인 + `run_pipeline.py`)과 서빙 구조(정적 자산이 곧 서비스, Supabase는 시민과학 전용)로 교체.
- `DATA_PIPELINE.md` 에 `cell_water` 가 **두 갈래로 쓰인다**는 점을 명시 — 판정 뒤 덧씌우는 수계 마스크(재적합 불필요)와 모형 변수 `sord`(값이 바뀌면 전량 재적합). 즉 `cell_water` 를 다시 만들면 9 → 11 → 12 단계를 이어서 돌려야 한다.
- R 실행 경로 표기를 실제 설치본(`R-4.5.0`)으로 정정 — 문서 두 곳이 존재하지 않는 `R-4.5.1` 을 가리키고 있었다.

## [0.9.0] - 2026-07-02

최초 버전 기준선 — 현재 라이브 상태를 정리한 스냅숏.

### 기능
- 발견공백 조회: 국가생물종목록(KTSN, 서비스 대상 40,156종) − 3원 관측 union(EcoBank·국립공원·GBIF)의 실시간 여집합으로 미발견·빈발견·지역/연도별 발견 현황 계산.
- 대문 대시보드: 분류군별 발견/미발견 종수, 국가적색목록 현황 도넛.
- 종별 검색·상세: 발견 지역 표시, 한반도 생물다양성(NIBR)·시민 제보(Naturing/EcoBank) 링크.
- 지도: 시도 ⇄ 시군구 토글 choropleth + 환경변수 오버레이(연평균기온·최난월·최한월·연강수·해발고도).
- 종 페이지: 발견지점 기후·고도 지위 막대(전국 분포 대비).
- 로그인(이메일 매직링크 + Google OAuth)과 관심종 저장(Supabase, 행 수준 보안).

### 배포
- GitHub Pages(main `/docs`, OpenStreetMap 배경) 상시 게시.

[Unreleased]: https://github.com/RachHus/Finding-gap/compare/v0.10.0...HEAD
[0.10.0]: https://github.com/RachHus/Finding-gap/compare/v0.9.0...v0.10.0
[0.9.0]: https://github.com/RachHus/Finding-gap/releases/tag/v0.9.0
