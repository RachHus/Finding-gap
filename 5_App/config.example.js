// 웹 클라이언트 런타임 설정 템플릿.
// 실제 파일 config.js 는 .env 에서 자동 생성됩니다(키 포함, git 제외).
//   생성: python 5_App/_make_config.py   (= .env 의 VWORLD_KEY 를 주입)
// vworld 키는 도메인(Referer) 잠금으로 보호되므로 클라이언트 노출은 허용됩니다.
window.VWORLD_KEY = "";
